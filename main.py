from encryptomatic import EncryptOMatic
from gui import GUI
from tkinter import *
from tkinter import ttk

print("This app uses a GUI made with tk")
print("If replit decides not to render it, please download an run the app")
print("A clean python 3 installation is all that is needed, no other dependencies")

root = Tk()
root.title("Encrypt-o-Matic 5000")
GUI(root)
root.mainloop()